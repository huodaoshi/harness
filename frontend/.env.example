# 复制为 .env.local。密钥请只放在 backend/.env

# 若设置则直连 backend（需 backend 开 CORS）；留空则走 next.config rewrites（推荐本地开发）
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8080

# rewrites 目标（未设置 NEXT_PUBLIC_API_BASE_URL 时生效）
API_PROXY_TARGET=http://127.0.0.1:8080
